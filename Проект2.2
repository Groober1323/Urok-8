#include <stdio.h>
#include <math.h>
main()
{
    float K,L;
    printf("Enter the coordinate of the point A:(K and L)\n");//пользователь вводит координату точки
    scanf("%f%f",&a,&b);
    if(a>=-2 && a<=0)
        {
            if(L=pow(-a+1,2)+1/b)
            {
                printf("Point A does fall into the area bounded by the circle\n");
            }
            else
                {
                printf("Point A does not fall into the area bounded by the circle\n");
                }
        }
    else
        {
            printf("Point A does not fall into the area bounded by the circle\n");
        }
    getchar();
}
